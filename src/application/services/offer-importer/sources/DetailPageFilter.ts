import { OfferSource } from 'src/application/services/offer-importer/sources/OfferSource'

/**
 * A decorator that goes to offer URLs, scans the page, and only keeps offers that match certain criteria.
 */
export class DetailPageFilter extends OfferSource {

}
